local accounts = {};

--TODO

--Takes the self param of the thing calling, do not call account:isLoggedIn
function accounts.isLoggedIn(self)

end

function account.tryNewUser(email, pass)

end

return accounts;